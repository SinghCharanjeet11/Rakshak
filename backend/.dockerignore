# The build context is uploaded to the daemon whole, so keep it small and free of
# secrets even though the Dockerfile only COPYs pyproject.toml and app/.
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
tests/
.env
.env.*
rakshak.db
*.db
